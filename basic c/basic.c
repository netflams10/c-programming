#include <stdio.h>

int main () 
{
    // printf("hello world");

    // variables
    // int age;

    // printf("%d", age);
    // age = 25;

    // int age = 25;

    // printf("\n%d", age);
    // printf("\nAge: %d", age);

    /**
     * int    -> 4 bytes -> %d   -> 2^32
     * double -> 9 bytes -> %lf
     * float  -> 4 bytes -> %f
     * char   -> 1 bytes -> %c  --->> are stored as integers
    */
//    double weight = 12.46;
//    float size_of_body = 18.45f;

//    printf("\n%lf", weight);        // added bunch of zeroes
//    printf("\n%.2lf", weight);      // only in 2 places decimal [note -> %.2lf]
//    printf("\n%f", size_of_body);
//    printf("\n%.1f", size_of_body);

//    double exponential_number = 5.5e6; // e6 -> means exponential 6
//    printf("\n%lf", exponential_number);

//     char simple_cahracter = 'z';

//     printf("\n%c", simple_cahracter);
//     printf("\n%d", simple_cahracter);

//     printf("\n Double size = %zu", sizeof(exponential_number));

    // Take Input from user

    // int age;

    // printf("Please input your age:");
    // scanf("%d", &age);

    // printf("%d", age);

    // double first_number;
    // double second_number;

    // scanf("%lf", &first_number);
    // printf("\n %lf %lf", first_number, second_number);

    return 0;
}